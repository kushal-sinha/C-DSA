single element -> odd , even approch 
                  first occurence at even index and second occurence at odd index
sorted and rotated array -> find which part is sorted and then apply the search accordingly
                            if the elements are same then you need to skip those duplicate elements
                            by doing s++ , e--
painter partition , book allocation problem                  